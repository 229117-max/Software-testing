# Завдання 1. Ролі, рівні та типи тестування

## 🔹 Ситуація
Команда робить форму логіну, в якій є:
- поле **Email**
- поле **Password**
- кнопка **Login**
- кнопка **Forgot password?**
- повідомлення про помилку при невірному вводі

---

## ❓ 1.1 Які ролі будуть задіяні у роботі над цією фічею і яким чином?

**Product Manager / Product Owner**
- пояснює навіщо це потрібно і як має працювати (які вимоги, що показувати при помилці)
- приймає результат, каже “ок/не ок”

**BA (якщо є)**
- деталізує вимоги: яка саме валідація email, які правила для паролю, які тексти помилок
- може описати сценарій “Forgot password?”

**UI/UX Designer**
- малює макет форми, стани (помилка, фокус, disabled)
- продумує, щоб було зручно і зрозуміло

**Frontend Developer**
- робить саму форму на фронті (поля, кнопки)
- додає валідацію на клієнті (наприклад, щоб email був схожий на email)
- підключає API логіну і показує повідомлення при помилці

**Backend Developer**
- робить/підтримує API для логіну і відновлення паролю
- перевіряє дані на сервері, віддає правильні помилки (401, 400 і тд)
- відповідає за безпеку (токени/сесії)

**QA Engineer**
- перевіряє, що все працює по вимогах
- пише тест-кейси/чекліст, ганяє позитивні і негативні сценарії
- може робити автотести (API/UI), якщо треба

**DevOps / SRE (інколи)**
- налаштовує середовища, логи, моніторинг
- може допомогти з rate limit / безпекою на рівні інфри

---

## ❓ 1.2 Які рівні тестування стосуються цієї фічі?

**Unit testing**
- тестування маленьких частин: функції валідації email/паролю, обробка помилок, окремі методи на бекенді

**Integration testing**
- перевірка, як фронт працює з беком (запит/відповідь)
- “Forgot password?” теж сюди, бо це інтеграція з бекендом + можливо поштою

**System testing (E2E)**
- повний сценарій як у реального користувача: відкрив сторінку → ввів дані → зайшов → побачив результат

**Acceptance testing (UAT)**
- перевірка, що фіча відповідає вимогам бізнесу/замовника (тобто “саме так ми і хотіли”)

---

## ❓ 1.3 Які типи тестування потрібно виконати для перевірки форми логіну?

### 🔹 Функціональні типи тестування

**Позитивні сценарії**
- правильний email + правильний пароль → логін успішний
- після логіну перекидає на потрібну сторінку (як задумано)
- кнопка “Forgot password?” відкриває потрібний екран/сторінку

**Негативні сценарії**
- неправильний пароль → показує помилку
- email не існує (якщо таке повідомляється) → помилка
- порожній email / порожній пароль → або помилка, або кнопка Login неактивна (залежить від вимог)

**Валідація полів**
- email без `@` / без домену → помилка
- пробіли в email (на початку/в кінці) → чи обрізає, чи лається
- дуже короткий/довгий пароль → очікувана реакція

**UI перевірки**
- правильно відображаються підказки і тексти помилок
- кнопка Login disabled/enabled коли треба
- Enter працює як submit (якщо передбачено)
- табом можна пройти по елементах

**API тестування**
- перевірити, що бек повертає правильні статуси (200, 400/401, 429 і тд)
- якщо сервер впав/таймаут → показує нормальне повідомлення, а не “білий екран”

**Regression**
- після правок перевірити, що логін не зламався і “Forgot password?” теж

---

### 🔹 Нефункціональні типи тестування

**Security (база для логіну)**
- багато невдалих спроб → чи є захист (rate limit/блокування) якщо потрібно
- повідомлення не має підказувати зайве типу “користувач існує/не існує”
- перевірка, що дані йдуть по HTTPS
- базові перевірки на XSS (вставити `<script>` у поле email і тд)

**Usability**
- чи зрозумілі тексти помилок (що саме не так і що зробити)
- “Forgot password?” легко знайти і зрозуміло куди воно веде

**Accessibility (A11y)**
- чи можна все зробити з клавіатури (Tab/Shift+Tab)
- фокус видно
- поля мають лейбли (щоб скрінрідер читав нормально)

**Compatibility**
- різні браузери (Chrome/Firefox/Safari) + мобільні (якщо актуально)
- різні розміри екранів, щоб не роз’їжджалось

**Performance (мінімально)**
- як швидко проходить логін при нормальній мережі
- що буде при повільному інтернеті (чи є loading)
